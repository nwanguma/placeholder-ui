import React from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

const ViewProduct = () => {
  return <Wrapper>this is the product</Wrapper>;
};

export default ViewProduct;
